# FLOW-12 M03-task-exception-approval

- 原始行：2588
- 原始最近标题：F4 任务异常与逾期补录
- 迁移方式：Mermaid block mechanical copy
- 当前定位：Current Product Flow；文件名为历史兼容标识。DEC-204 已取消任务异常与逾期补录审批，当前只表达 M03 校验确认后的直接生效流程。

```mermaid
flowchart TD
    A[当前执行人或admin操作延期/暂停/取消] --> B{权限、字段、状态、日期、影响和版本校验}
    B -- 不通过 --> F[任务保持原状并显示业务错误]
    B -- 通过 --> E[用户确认]
    E --> G{变更类型}
    G -- 延期 --> H[直接更新当前截止与提醒]
    G -- 取消 --> I[直接退出有效任务集合并保留历史]
    G -- 暂停 --> J{确认时仍未逾期且从未逾期?}
    J -- 否 --> K[暂停不生效;任务保持当前逾期]
    J -- 是 --> L[常规/生日/节假日进入待执行或暂停;<br/>不形成逾期和健康风险]
    L --> M{暂停任务类型与时间}
    M -- 常规到恢复日 --> N[按恢复日+当前职级当前周期<br/>重算当前截止并恢复待执行]
    M -- 生日/节假日截止前恢复 --> O[沿用原当前截止并恢复待执行]
    M -- 生日/节假日越过截止 --> P[自动关闭并退出有效集合]
    Q[逾期补录:实际时间在截止内+证明齐全] --> R[用户确认后直接形成正式记录<br/>认定按期并保留曾经逾期]
```

> Historical / Replaced：本文件旧 Current 内容包含区域总监、市场副总或总裁审批，以及`已通过-业务处理失败`。这些语义已被 DEC-204 替代；旧内容只用于 Git/Archive 追溯。

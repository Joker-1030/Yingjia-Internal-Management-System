# FLOW-11 M02-key-contact-transfer-approval

- 原始行：2568
- 原始最近标题：F3 关键人调岗
- 迁移方式：Mermaid block mechanical copy
- 当前定位：Current Mechanical Evidence；DEC-164 确认关键人调岗实际生效取审批通过时刻与计划调岗日 00:00 的较晚值，等待期间展示“已通过待生效”。业务失败时审批结论仍为已通过，只允许 admin 受控重试。

```mermaid
flowchart TD
    A[发起关键人调岗] --> B{变更范围}
    B -- 同一客户单位内改部门/岗位 --> C[所属区域总监审批]
    B -- 同省公司内换单位 --> D[市场副总审批,角色空缺时总裁]
    B -- 调至其他市/区县公司 --> E[目标PM接收确认<br/>目标PM即当前PM时跳过]
    B -- 调至其他省公司 --> F[目标区域总监审批]
    E --> G[目标区域总监审批]
    C --> H{结果}
    D --> H
    F --> H
    G --> H
    H -- 驳回/撤回 --> I[原任职不变]
    H -- 通过 --> J{到计划调岗日 00:00?}
    J -- 未到 --> K[已通过待生效<br/>审批结论仍为已通过,原任职继续有效]
    K --> J
    J -- 已到 --> L{生效时任职/责任/岗位仍合法?}
    L -- 失效 --> M[已通过-业务处理失败<br/>原任职保持,仅admin受控重试]
    L -- 合法 --> N[关闭原任职,建立新任职<br/>责任与覆盖KPI按新任职重算]
```

> Historical / Replaced：本 Flow 旧机械块在审批通过后直接进入生效校验，且失败分支包含 `admin关闭`。DEC-163 已替代关闭动作，DEC-164 已补齐计划调岗日等待分支；旧表达只用于追溯，不再作为 Current Rule。

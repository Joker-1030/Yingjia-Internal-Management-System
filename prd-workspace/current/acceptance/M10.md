# M10 Acceptance Criteria

> Mechanical migration from the current full PRD. Stable IDs, priority, and row order are preserved. 2026-08-22 Source Governance（DEC-163）补充当前产品边界：单工作表最多 5,000 条业务行，5,001 条及以上批次级拒绝；导入按单行原子处理，允许不同业务行分别成功或失败。

| AC 编号 | Given | When | Then | 优先级 |
| --- | --- | --- | --- | --- |
| AC-F015-07 | 用户上传 `.xls`、宏、加密、超 20 MB，或任一工作表超过 5,000 条业务行的文件 | 发起预校验 | 批次级拒绝并指出文件或工作表及具体原因，不解析或写入任何业务数据；5,000 行允许，5,001 行及以上拒绝 | P0 |
| AC-F015-08 | admin 全量模板或 PM 关键人模板缺少对应工作表、模板版本或必需表头 | 发起预校验 | 按模板类型判断必需结构，批次标记失败并列出缺失项，不进入待确认 | P0 |
| AC-F015-09 | 客户或关键人行含错误手机号、生日、职级、显式组织上级、公司/部门/标准岗位路径或业务责任区划深度 | 预校验 | 报告精确到行号、字段、错误码和修复建议；跨集团、自身/后代/成环上级及非法业务责任均被阻止，正确行仍归类可导入 | P0 |
| AC-F015-10 | 工作簿内或存量存在唯一键重复 | 预校验 | 工作簿内重复为阻断错误；存量命中为疑似重复并默认跳过，不自动覆盖 | P0 |
| AC-F015-11 | PM 关键人模板含本人负责客户和外范围客户行 | 预校验 | 本人负责范围行正常分类；外范围行只提示超范围，不返回外范围客户、关键人或负责人详情 | P0 |
| AC-F015-12 | 批次同时有可导入、重复和错误行 | 用户确认导入 | 每一业务行独立成功或失败，仅可导入行按依赖顺序写入，失败行不回滚其他成功行；批次为部分成功，结果统计与业务对象数量一致 | P0 |
| AC-F015-13 | 导入请求前端超时但后台完成 | 用户刷新并再次点击确认 | 返回同一批次最终结果，幂等键阻止重复客户、关键人或责任关系 | P0 |
| AC-F015-14 | 导入完成 | 下载结果报告 | `.xlsx` 包含汇总和每行处理结果、对象 ID、错误字段、错误码、原因及建议，且下载重新鉴权 | P1 |
| AC-F015-15 | admin 地市负责人表同时含未分配和已分配城市 | 预校验并确认 | 未分配城市可初配；已分配城市一律提示走 F004 交接且不覆盖，其他行结果不受影响 | P0 |
| AC-F015-16 | 总裁、市场副总、区域总监、HR、PM 和 admin 分别访问数据导入 | 查看菜单、模板并伪造上传 | 区域总监可长期使用本人省级业务责任客户关键人模板，并在初始化期使用本人省级业务责任客户项目模板；PM 可使用本人市级/区县级业务责任客户关键人模板，并在初始化期使用本人项目模板；admin 可使用全量、关键人及初始化项目模板；总裁、市场副总和 HR 无入口且伪造请求返回 403 | P0 |
| AC-F015-17 | PM 下载关键人模板后失去某地市责任，或模板候选被停用 | 使用旧下拉值上传 | 预校验按上传时最新范围和状态拒绝对应行并建议重新下载，不因旧模板曾提供候选而放行 | P0 |
| AC-F015-18 | 用户下载两类 V1.5 Excel 模板 | 检查并填写枚举及范围字段 | 组织上级类型、同集团上级客户公司、业务责任区划、客户单位、客户部门和标准岗位提供规定的联动下拉；客户单位工作表必填“客户公司编码”和“组织上级类型”，选择客户公司时条件必填“上级客户公司编码”，选择集团公司时该字段必须空且以所属集团为上级；客户部门工作表必填“所属客户公司编码”；缺失、未知、跨集团、与名称不一致或手工绕过下拉填写不匹配路径时整行被拒绝 | P1 |
| AC-F015-19 | 关键人导入行选择标准岗位 | 分别漏填岗位、填写未知岗位编码、跨部门岗位编码和合法同路径岗位编码 | 前三种精确报错且不写入；合法路径写入稳定岗位 ID，不自动新增客户部门或标准岗位 | P0 |
| AC-F015-20 | admin 模板同时包含新集团、新地市负责人、新客户、新部门和新关键人 | 确认导入 | 严格按集团 > 地市负责人 > 客户单位组织上级依赖 > 客户部门 > 关键人处理；客户创建时显式上级已唯一解析且业务责任已能解析负责人，部门失败只阻断依赖关键人，不因文档顺序产生伪失败 | P0 |
| AC-F015-21 | admin 五个业务工作表合计包含 8 条确认可导入源业务行，最终 6 行全部写入成功、2 行任一必需对象写入失败 | 查看分工作表及全批次指标、列表和结果报告 | 全批次客户档案导入成功率显示 75.0%，分子分母等于五个工作表源业务行汇总且与结果报告一致；每行是最小原子，一行写入多个关联对象仍只计 1 行且不得产生半成品，失败行不回滚成功行；分母为 0 显示 `--`，重复、阻断和未确认行不进入分母 | P1 |
| AC-F015-22 | admin 范围内存在不同模板、状态、创建人、日期及异常数量的导入批次 | 分别使用单项筛选、状态多选和跨维度组合筛选，再执行重置 | 单项结果符合字段规则；状态多选取并集、跨维度取交集；查询后回到第 1 页，重置后恢复公司全部有权批次及默认排序 | P0 |
| AC-F015-23 | admin、当前 PM 和另一名 PM 均有历史导入批次 | admin 与当前 PM 分别查看列表、筛选创建人并直接请求另一名 PM 的批次及报告 | admin 可按员工筛选公司批次；当前 PM 不显示创建人筛选且只返回本人批次，直接请求他人批次或报告返回 403，不泄露文件名、状态、数量或是否存在 | P0 |
| AC-F015-24 | PM A 下载本人范围的关键人模板并转交 PM B | PM B 上传该模板，随后自行下载新模板上传 | 他人下载模板因下载人标识与当前上传人不一致被批次级拒绝且不解析行；本人新模板可继续预校验，admin 全量模板不受 PM 模板限制 | P0 |
| AC-F015-25 | 项目初始化导入开放 | admin、区域总监和 PM 分别下载并上传项目模板 | admin 可导入公司项目，区域总监仅导入本人负责省级业务责任客户项目，PM 仅导入本人负责市级/区县级业务责任客户项目；无有效负责人或越权行被阻断且不泄露其他范围事实 | P0 |
| AC-F015-26 | 项目初始化完成 | 各角色查看模板、尝试新上传并查看历史批次 | 项目模板隐藏且新上传被拒绝；既有批次和报告长期保留，按 admin 公司、区域总监本区域、PM 本人创建范围查看 | P0 |
| AC-F015-27 | 项目模板包含开始/结束时间、项目确认天数、其他项目基本字段、历史阶段、原创建时间及多个成功/失败行 | 确认导入 | 每行独立成功或失败；项目确认天数写入 M11 同名字段，系统按开始/结束时间自动计算并写入只读系统计算天数，培训项目金额和结账金额按项目确认天数计算；系统同时匹配地区/负责人，失败行不取号，成功行按原创建年份生成新 `XM` 编号且不使用原历史编号 | P0 |
| AC-F015-28 | 导入阶段与开始/结束时间或项目类型生命周期不一致，或历史项目缺当前完成条件 | 预校验并导入 | 不一致行阻断且不静默改写；历史已完成但资料/满意度不齐的项目按已交付建立并显示待办 | P0 |
| AC-F015-29 | 项目模板命中当前占用项目名称、已有项目或已取消项目 | 确认导入 | 当前占用/存量命中行跳过且不覆盖；已取消项目名称可复用；不允许导入已取消阶段，也不提供批量编辑或取消 | P0 |
| AC-F015-30 | 区域总监分别导入本人负责省级业务责任客户关键人和本区域市级/区县级业务责任客户关键人 | 上传关键人模板并查看批次 | 省级业务责任客户关键人长期可导入且本人只看本人创建批次；市级/区县级业务责任客户行被拒绝；该能力不随项目初始化导入关闭 | P0 |
| AC-F015-31 | admin 的集团公司工作表同时包含可成功创建、可匹配存量和写入失败的集团行 | 检查模板、确认导入并查看结果报告 | 模板没有集团编号输入列；成功创建行由 M09 生成系统内唯一的 `CG` + 8 位编号，存量命中行沿用原编号，报告返回对应正式集团编号；失败行不成立集团对象且不显示成功编号；已成功分配的编号不因批次关闭或集团后续停用、恢复而回收 | P0 |
| AC-F015-32 | 同一批次包含成功且适用 M03 规则的对象、失败行，并发生一次任务生成失败后重试 | 查看批次、行结果和 M03 任务 | 成功且适用对象在导入成功后 5 分钟内生成唯一适用任务；失败行不生成任务；生成失败不改变导入成功结果，重试不重复生成任务 | P0 |

## Source Governance Coverage

- AC-F015-07 的“超过行数”当前唯一阈值为单工作表 5,000 行：5,000 允许，5,001 及以上拒绝。旧完整 PRD 的 10,000/工作表和 50,000/工作簿作为 Historical / Replaced Evidence 保留。
- AC-F015-12/21 的“部分成功”按单行原子解释：每一业务行独立成功或失败，失败行不回滚其他成功行；一行内对应业务对象不得产生半成品。旧 FLOW-07“按工作表原子”只作 Historical / Replaced Evidence。
- AC-F015-18 的客户部门父级定位由 DEC-164 闭合：客户单位工作表必填“客户公司编码”，客户部门工作表必填“所属客户公司编码”，同一稳定编码是存量或同批次父公司定位依据；旧字段表省略该列并依赖名称组合定位的表达只作 Historical / Replaced Evidence。
- AC-F015-16 已按 DEC-177、DEC-180 收口为当前唯一角色边界；区域总监只开放本人省级业务责任客户关键人长期导入与初始化期本人省级业务责任客户项目导入，其他导入仍无权。
- AC-F015-25 至 AC-F015-30 是本次新增项目管理后的 Current Acceptance；既有 AC ID、优先级和顺序不变。
- AC-F015-31 固化 DEC-179 的集团导入编号边界：编号不作为模板输入，由 M09 在集团成功创建时生成并由结果报告返回。
- AC-F015-32 固化 DEC-190 的 M10→M03 结果边界：5 分钟内生成、失败行排除、生成失败不回滚导入且重试幂等。
- AC-F015-09/18/20 按 DEC-180 使用显式组织上级与独立业务责任事实；模板不再包含独立公司层级或公司所在地省市区字段。

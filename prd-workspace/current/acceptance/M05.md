# M05 Acceptance Criteria（已移除）

> Current Boundary：M05 独立“停用记录”能力已取消，Product Status 为`REMOVED`。本文件只验证移除边界，不定义可开发页面、状态、权限或流程。
>
> Historical Boundary：原 `AC-F012-06` 至 `AC-F012-18` 已被 `DEC-207` 替代，仅通过 Git 历史和归档证据追溯，不再作为 Current Acceptance。

| AC 编号 | Given | When | Then | 优先级 |
| --- | --- | --- | --- | --- |
| AC-M05-REMOVED-01 | 任一角色登录当前产品 | 查看菜单、权限树或尝试访问旧 M05 路由 | 不显示 M05 菜单或权限节点；旧路由按不存在的当前能力处理且不返回业务数据 | P0 |
| AC-M05-REMOVED-02 | admin 执行当前已正式存在的停用/恢复操作 | 查看入口、确认、生效结果和历史 | 操作继续在 M02、M06、M09 等对象 Owner 中按现行权限、校验和失败保护执行；不创建 M05 记录或跨模块历史副本 | P0 |
| AC-M05-REMOVED-03 | 员工停用导致任务、项目或商机责任变化 | 查看后续历史 | M03、M11、M12 分别在自身对象历史中记录来源、摘要和结果；不存在 M05 详情下钻 | P0 |
| AC-M05-REMOVED-04 | 研发或 Agent 发现旧 M05 PRD、Flow、Demo 容器、代码或历史证据 | 判断当前实现范围 | 仅作为 Historical / Replaced Evidence，不恢复页面、路由、权限、状态、流程或统一历史中心 | P0 |

精确依据：[`DEC-207《取消独立停用记录并由业务所有者保留历史》`](../../decisions/DEC-207-取消独立停用记录并由业务所有者保留历史.md) -> “决策”第 1-8 项。

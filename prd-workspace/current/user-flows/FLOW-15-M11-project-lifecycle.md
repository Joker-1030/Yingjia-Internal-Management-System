# FLOW-15 M11-project-lifecycle

- 当前定位：Current Product Flow
- 规则来源：M11、DEC-174、DEC-176

## 当前人工创建阶段

```mermaid
flowchart TD
    A[有权人员选择客户并填写项目] --> B{客户业务责任能否唯一匹配有效负责人?}
    B -- 否 --> C[阻止立项且不生成项目编号]
    B -- 是 --> D{名称、时间、资源、金额和人员校验通过?}
    D -- 否 --> C
    D -- 是 --> E[生成 XM 年度编号并进入已立项]
    E --> F{到达开始时间?}
    F -- 是 --> G[自动进入进行中]
    G --> H{项目类型}
    H -- 培训项目到达结束时间 --> I[自动进入已交付]
    H -- AI 软件项目 --> J{项目负责人确认交付?}
    J -- 否且已到期 --> K[P01 每日提醒并按规则升级]
    K --> J
    J -- 是 --> I
    I --> L[按缺项展示资料待上传/待评价/待回款]
    L --> M{必填资料和适用满意度齐全?}
    M -- 否 --> L
    M -- 是 --> N[自动进入已完成]
    N --> O[仅可新增非必填可选资料]
    E --> P[有权人直接取消]
    G --> P
    I --> P
    P --> Q[已取消:只读/清除待办和提醒/释放排期]
```

## 未来商机与合同能力上线后

普通人工创建入口关闭。只有合同审核并验证通过后，商机负责人可从商机点击`创建项目`；系统带出商机、合同、客户和按客户业务责任解析的项目负责人，补齐项目字段并通过同一立项校验后直接进入`已立项`，不增加项目立项审批。客户显式组织上级和组织路径不参与负责人解析。后续生命周期与上图一致。

`资料待上传`、`待评价`、`待回款`是可并行待办标识，不是主阶段。`待回款`当前不能解除，也不阻塞项目完成。当前取消直接生效；未来统一审批中心接管前不得显示为已有取消审批能力。

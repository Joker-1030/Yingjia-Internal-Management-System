# M03 Acceptance Criteria

> Mechanical migration from the current full PRD. Stable IDs, priority, and row order are preserved. Product-owner-confirmed wording replacements are governed below and retain their prior wording as Historical / Replaced Evidence.

| AC 编号 | Given | When | Then | 优先级 |
| --- | --- | --- | --- | --- |
| AC-F008-33 | 当前范围存在全部五状态及取消执行记录 | 查看父任务总览和执行明细 | 五状态互斥合计等于有效执行记录总数，取消不计入；父任务编号、子执行编号与各自详情一致 | P0 |
| AC-F008-34 | 任务在截止前、逾期后分别提交维系 | 完成任务 | 前者按期；后者按实际时间进入补录审批或逾期补完成，三个时间和曾逾期事实可查 | P0 |
| AC-F008-35 | 已逾期任务倒填当前截止内时间 | 未填原因、未传证明或传入非法文件 | 均阻止申请；字段和文件合法后进入补录审核中，不提前完成或续期；原截止只保留审计 | P0 |
| AC-F008-36 | 专项维系或覆盖 KPI 执行项仍未完成 | 尝试申请单项延期或人工完成覆盖 KPI | 页面无入口，伪造请求返回状态冲突；截止时间仍等于专项结束时间，覆盖 KPI 仅由所选覆盖维度对应的部门或岗位覆盖事实自动完成 | P0 |
| AC-F008-37 | 同一任务执行记录生成调度重复执行或回调重试 | 查看有效执行记录 | 唯一键保证同一计划/事件只有一条有效执行记录，复用原父任务号和原子执行编号且不重复通知 | P0 |
| AC-F008-38 | 管理员代办 PM 或区域总监任务 | 填写代办原因并完成 | 执行人仍是原业务执行人，创建人/实际操作人显示管理员，两者及原因进入日志 | P0 |
| AC-F008-39 | 任一常规计划、生日年月、节日年度、专项维系或覆盖 KPI 首次建立正式父任务并生成执行记录 | 查看总览、执行明细与详情 | 父任务获得唯一 `MT` + 8 位任务编号；专项发布后与其一对一父任务共用该编号且不存在独立专项业务编号；执行记录获得 `任务编号-6位父内流水`，列表可按任务编号或任务执行记录编号搜索，详情分别显示且用户不能编辑 | P0 |
| AC-F008-40 | 同一父任务并发生成 30 条不同执行记录，且另有调度重试命中同一执行业务唯一键 | 查看父任务与编号序列 | 不重复建立父任务；不同执行记录子编号在父内不重号，重复业务返回既有执行记录及原父/子编号，失败事务允许断号 | P0 |
| AC-F008-41 | 一条执行记录完成、取消或关闭，随后又生成下一期执行记录 | 查询父任务及全部历史执行记录 | 父任务编号始终不变；历史子编号永久保留且不复用；下一期仅获得同一父任务下新的递增 6 位子编号 | P0 |
| AC-F008-42 | 父任务、执行记录和维系记录均发生过业务修改，随后列表成功加载或手动刷新 | 查看任务总览、执行明细、维系记录和各自详情 | 三张列表均不展示行级更新时间，并在表格右上角展示最近一次成功加载或刷新的“数据更新时间”；详情展示对象自身创建/更新时间，纯查看、消息已读和统计重算不改变对象更新时间 | P1 |
| AC-F008-43 | 常规维系任务已超过当前截止 400 天且从未完成 | 任务扫描多次后由 PM 提交实际时间为当天的维系记录 | 扫描期间始终为当前逾期且不存在补完成截止或下一期；提交成功后认定逾期后补完成，并按实际维系日期加当前职级周期生成唯一下一期，曾经逾期和首次逾期事实保留 | P0 |
| AC-F008-44 | 生日/节假日规则均固定不允许逾期补完成 | 任务越过截止并在第 5 天、第 8 天提交 | 两类任务越过截止均进入已过期未完成；第 5 天和第 8 天均无补完成入口并拒绝提交，且不误走逾期补录审批 | P0 |

| AC-F008-45 | 发布后未开始、有效期内进度 60.0%、有效期内进度 100.0%、结束后进度 100.0% 和结束后进度不足 100.0% 各一条 | 查看专项列表、任务总览和父任务详情 | 专项不提供草稿状态；专项与唯一父任务分别同为待开始、进行中、进行中、已结束和已结束，任务主题使用同一状态；完成进度不改变时间生命周期，零执行项只显示空态而非第四个父任务状态 | P0 |
| AC-F008-46 | 同一关键人的常规任务长期当前逾期 | 分别让定时扫描、主动维系入口、页面列表处理该任务 | 定时扫描不转已过期且不建下一期；主动维系仍唯一匹配并可逾期补完成；已存在任务保留原截止与提醒行或当前逾期；列表持续标红并可进入处理 | P0 |
| AC-F008-47 | 常规任务当前逾期，随后关键人停用或调岗审批生效 | 查看原任务、目标责任计划和任务历史 | 停用按审批结果关闭原任务；调岗按规则关闭原任务并在目标责任下重建唯一常规计划；两者均保存流程编号、原因和历史，不把原任务伪记为已完成或已过期未完成 | P0 |
| AC-F008-48 | 同一区域存在省级和市级/区县级业务责任客户的有效关键人 | 运行常规、生日、节假日、专项维系及覆盖 KPI 生成并查看异常申请路由 | 省级业务责任客户五类执行项均分配所属区域总监且不经过 PM，异常申请由市场副总或总裁审批；市级/区县级仍分配当前 PM 并由区域总监审批 | P0 |
| AC-F008-49 | 任务原截止为 3 月 1 日，延期后的当前截止为 3 月 10 日，系统在 3 月 11 日登记维系 | 实际维系时间分别填写 3 月 8 日和 3 月 11 日 | 3 月 8 日进入逾期补录审批并通过后认定按期；3 月 11 日认定逾期后补完成；原截止只在延期历史和审计中展示 | P0 |
| AC-F008-50 | 在职员工名下同时存在多类未完成任务、已完成任务、已取消任务和当前审批节点 | HR/人事或 admin 填写原因并确认停用员工，随后恢复员工 | 停用与该员工全部未完成任务取消同次直接生效，取消记录保留原执行人、稳定编号、停用原因、实际操作人、时间和员工停用关联，不逐条建立任务取消审批；已完成/已取消任务和当前审批节点不被改写，恢复不重开停用时取消的任务 | P0 |
| AC-F008-51 | M10 同一批次包含成功且适用 M03 规则的对象、失败行，并发生一次任务生成失败后重试 | 查看导入结果和 M03 任务 | 成功且适用对象在导入成功后 5 分钟内生成唯一适用任务；失败行不生成任务；生成失败不回滚导入成功，重试复用同一业务唯一键且不重复生成父任务或执行记录 | P0 |
| AC-F008-52 | 当前权限范围内同时存在常规维系、专项维系、关键人覆盖 KPI、生日关怀和节假日关怀父任务；四类有限任务的进度包含 `--`、0.0%、40.0%、80.0% 和 100.0% | 查看 M03 任务总览并使用最低、最高及双边进度筛选 | 筛选项仅为“任务编号”“任务标题”“任务类型”“任务状态”“最低任务完成进度”“最高任务完成进度”，不展示目标范围或执行人筛选；常规维系进度固定显示 `-`，四类有限任务只显示 1 位小数百分比或 `--`，均不显示分子/分母或进度条；范围筛选包含边界、支持单边，并排除常规维系的 `-` 和有限任务零分母的 `--`；最低值高于最高值时不筛选并提示“最低进度不能高于最高进度”；列表继续不展示目标范围和行级更新时间，开始/结束时间及其他数据口径、权限和操作不变 | P1 |
| AC-F008-53 | 当前授权范围内某关键人定期维系计划包含五类有效执行记录及已取消、已关闭记录；完成一条常规维系后会生成下一期执行记录 | 查看任务总览和该常规维系的数据看板，完成一条任务并再次查看 | 任务总览在完成前后均显示 `-`，数据看板不展示“任务完成进度”指标卡；已完成、待执行 / 暂停、当前逾期、补录审核中、已过期未完成等数量继续按有效集合实时展示，已取消和已关闭不进入这些有效数量；完成与下一期生成规则不变，新执行记录进入后只更新对应执行数量，不产生常规维系百分比 | P0 |
| AC-F008-54 | 五类任务均已按 M09 规则或专项配置生成父任务和执行记录 | 从任务总览分别打开五类父任务，再从一级执行明细打开单条任务执行详情 | 五类父任务 Drawer 标题栏均只显示“任务详情”和关闭入口，并固定包含“任务详情、数据看板、执行明细”三个页签；名称区只显示任务名称和一处任务状态且无编号、类型或有效期副标题；任务编号、任务类型、开始时间、结束时间、更新时间及类型专属字段只在“任务详情”页签展示，字段区不重复任务名称和状态，详情分类统一称为“任务类型”；关键人定期维系计划不展示任务标题模板、执行要求或任务完成进度指标卡，但保留执行数量指标，其他类型按适用规则展示已有详情、范围和完成进度；Drawer 内执行明细保留表格但无操作列、任务详情或维系记录下钻，一级执行明细及单条任务执行详情继续展示渲染标题和保存的执行要求 | P1 |
| AC-F008-55 | 当前授权范围存在常规维系、专项维系、生日/节假日和覆盖 KPI 执行记录，并包含今日到期、未来第 1-7 日待执行、当前逾期、暂停、补录审核中、已过期未完成及曾经逾期记录 | 查看一级执行明细的筛选和列表，分别选择“当前逾期风险”“未来 7 日到期”，并查看覆盖 KPI 行 | 筛选仅展示任务编号、任务执行记录编号、任务标题、任务类型、关键人名称、客户公司、执行人、截止开始/结束日期和风险范围，五类当前分类仍由页签承载；风险范围只有全部、当前逾期风险和未来 7 日到期，前者仅命中常规/生日/节假日当前逾期且明确排除暂停任务，后者仅命中截止晚于今天且不晚于未来第 7 个自然日的待执行记录，两者互斥且不出现曾经逾期或无当前风险选项；列表列名为“执行目标”和“客户范围”，常规维系、专项维系、生日关怀和节假日关怀分别展示关键人与客户公司，覆盖 KPI 分别展示目标部门/岗位与 `共 N 个客户`，该数量不可点击且客户明细只从任务详情的数据看板进入 | P1 |
| AC-F008-56 | 当前存在未逾期且从未逾期、当前逾期、曾经逾期的常规/生日/节假日任务，以及专项维系和覆盖 KPI | 分别打开任务变更并伪造暂停申请；再让一条合法暂停申请在审批期间越过截止后通过 | 只有未逾期且从未逾期的常规/生日/节假日任务显示并接受暂停；其余任务无暂停选项且请求被拒绝。审批生效时再次校验，已逾期任务不暂停并保持当前逾期，审批实例进入既有已通过-业务处理失败链路 | P0 |
| AC-F008-57 | 当前未逾期且从未逾期的常规、生日和节假日任务分别获批暂停 | 让常规任务到达恢复日；让生日任务在截止前恢复；让节假日任务暂停越过当前截止 | 三类任务暂停期间均归入待执行 / 暂停，不形成当前逾期、曾经逾期、首次逾期时间或健康风险。常规任务按恢复日、关键人恢复时当前职级及当前有效周期计算新当前截止至当日 23:59:59，并保留原截止及新旧截止、职级、周期历史；生日任务恢复待执行并沿用原当前截止；节假日任务自动关闭、退出有效集合和完成进度分子/分母，且不进入已过期未完成 | P0 |
| AC-F009-20 | 发布人填写专项表单 | 输入超长标题、结束早于开始或允许补完成却不填截止 | 对应字段报错且不能发布，当前表单内容保留供修正，不创建专项、父任务、任务编号或执行记录 | P0 |
| AC-F009-21 | 所有目标维度为空 | 点击发布 | 展示当前全范围预计关键人数并要求显式勾选确认；未确认不能发布 | P0 |
| AC-F009-22 | 有效期内新增、调岗或修改职级使关键人进入/退出范围 | 等待增量匹配和小时兜底 | 5 分钟内增量更新，兜底无重复；每项保留规则版本与判定原因 | P0 |
| AC-F009-23 | 专项尚未结束或已结束 | 修改结束日期 | 结束前统一同步全部有效执行项并重算；结束后拒绝修改；单项无独立截止 | P0 |
| AC-F009-24 | 专项维系含各种完成认定和当前状态 | 查看详情看板并下钻 | 总完成率、按期完成率和五状态均可与同一执行项集合核对；覆盖 KPI 页面不混用该维系完成率 | P0 |
| AC-F009-25 | 总裁选择中国移动集团、市级业务责任客户、岗位覆盖和某标准岗位发布覆盖 KPI | 查看发布预览并正式发布 | 分集团展示分母、已覆盖、当前覆盖率和需新增数；发布后每名有目标客户的 PM 生成 1 条唯一 KPI 待办，详情逐项列出已覆盖和未覆盖市级业务责任客户 | P0 |
| AC-F009-26 | 覆盖 KPI 有效期内某单位最后一名符合目标部门/岗位的关键人停用并使责任人覆盖率低于目标，随后新增合法关键人 | 等待增量重算 | 原责任人执行项先重新打开并通知，恢复达标后自动完成；始终复用同一执行项编号且记录失去/恢复达标时间 | P0 |
| AC-F009-27 | 覆盖 KPI 分别选择省级、市级、区县级业务责任客户 | 发布并查看执行人 | 省级业务责任客户执行人是区域中心主管，市级/区县级执行人是地市负责人 PM；无有效责任人的单位列为发布阻断项 | P0 |
| AC-F009-28 | 总裁、市场副总或 admin 查看覆盖 KPI 发布前预览或已发布专项详情 | 未点击发布，随后从 M03 专项管理进入“发布专项任务” | 仅查看不生成待办；填写集团、业务责任层级、覆盖维度、稳定目标 ID、目标值、业务责任范围和有效期并正式发布后才生成任务；M01 工作台不提供覆盖分析或发布入口 | P0 |
| AC-F009-29 | 市场副总选择“部门覆盖”和目标部门发布 KPI | 预览、发布并查看执行明细 | 分子仅统计当前任职部门 ID 等于目标部门的有效关键人所在单位；岗位不同不影响，按责任人生成唯一聚合待办 | P0 |
| AC-F009-30 | 发布人选择“岗位覆盖”但目标范围内没有所需标准岗位 | 尝试输入自由文本或直接发布 | 页面不提供自由文本入口并提示先由 admin 在客户组织树维护岗位；接口拒绝未知岗位 ID，不生成专项或待办 | P0 |
| AC-F009-31 | 覆盖 KPI 发布表单已选择部门及目标部门 | 切换为岗位覆盖再切回部门覆盖 | 切换时清空另一维度目标值并要求重新选择；发布快照只保存最终维度和值，不残留双重匹配条件 | P0 |
| AC-F009-32 | 专项维系范围内同时有采购决策人和非采购决策人 | 分别选择全部、仅采购决策人、排除采购决策人发布预览 | 三次预计人数和明细严格按当前字段值匹配且可核对；该筛选不改变关键人普通列表的采购决策人优先排序 | P0 |
| AC-F009-33 | 责任人分母为 3，目标覆盖率为 66.7% | 已覆盖单位数从 2 变为 3 | 目标需覆盖数向上取整为 3；已覆盖 2 时不达标、3 时达标，页面百分比与整数判断同源且不因浮点误差提前完成 | P0 |
| AC-F009-34 | 同一专项发布请求超时并重试 | 查询专项、父任务和执行项 | 只存在一个专项、一个一对一父任务、二者共用的一个 `MT` 任务编号和一组唯一执行项；不生成独立专项业务编号，状态一致且重试不重复取号 | P0 |
| AC-F009-35 | 待开始、进行中和已结束专项各一条，其中进行中与已结束均包含进度 100.0% 样例 | 查看专项列表并逐一按状态筛选，再打开详情和关联父任务 | 列表状态只展示待开始、进行中和已结束，不提供草稿状态；三种状态的专项与父任务按时间生命周期保持一致，进度 100.0% 不提前结束，列表、筛选和详情均使用同一任务编号且不出现双编号字段 | P0 |
| AC-F009-36 | 专项维系和覆盖 KPI 的有效期均已过 50%，且分别有已完成执行项和已达标责任人 | 计算重点动态的低进度阈值，随后将任一专项分母变为 0 | 专项维系使用总完成率，覆盖 KPI 使用已达标责任人数/有效责任人数；页面明确中文口径，分母为 0 显示 `--` 且不触发低进度动态 | P0 |
| AC-F010-22 | 新增维系记录 | 方式为其他但无说明，或摘要/反馈少于 5 字 | 字段级报错并保留其他输入，不写记录或任务 | P0 |
| AC-F010-23 | 单记录已有 9 个附件或 100 MB | 再传文件，或上传扩展名/MIME/文件头不一致文件 | 系统拒绝非法或超限文件，保留已通过附件，不返回永久地址 | P0 |
| AC-F010-24 | 附件关联成功但任务状态更新失败 | 提交任务完成 | 整体不生效，任务仍未完成，表单与临时附件可重试且不重复上传记录 | P0 |
| AC-F010-25 | PM 主动记录命中 0、1、2 条有效常规任务 | 分别提交 | 0 条仅保存；1 条同事务闭环；2 条阻止并告警，不任意选择任务 | P0 |
| AC-F010-26 | 已保存维系记录分别关联任务和未关联任务 | 打开“追加附件”入口并尝试修改关键人、任职/公司、维系方式、维系时间、摘要、反馈、下一步计划、关联任务或维系人，再追加附件 | 两类记录的全部业务字段均只读且伪造修改被拒绝；仅合法新附件追加成功，不改变任务完成认定、不重新匹配任务，已有附件不能删除或替换 | P0 |
| AC-F010-27 | 管理员独立录记录或代办任务 | 保存后查看详情 | 独立记录维系人为管理员；代办记录同时展示原业务维系人、管理员录入人和代办原因 | P0 |
| AC-F010-28 | 有附件权限和无附件权限用户分别打开同一图片、PDF 和 Office 附件 | 预览、下载、尝试删除/替换已有附件或为已保存记录追加附件 | 有权用户可在线预览图片/PDF，Office 显示元数据并通过 5 分钟单次签名地址下载；已有附件无删除或替换入口且伪造请求不生效，合法新附件只追加；无权请求统一 403 且不返回永久对象地址 | P0 |
| AC-F010-29 | 当前权限范围内存在不同关键人、客户公司、维系方式、维系人、维系日期及任务关联情况的正式维系记录 | 查看维系记录筛选并组合查询 | 筛选仅展示记录编号、关键人名称、客户公司、维系方式、维系人、维系开始日期、维系结束日期和关联任务；开始/结束日期共同表达维系日期范围，各条件只匹配自身字段并取交集，不展示关键人编号、沟通摘要、区域、城市、附件、创建日期或记录状态筛选 | P1 |

> Source Governance（2026-08-22，DEC-163）：AC-F008-44 的历史 Given/Then 曾包含“一类允许 7 天补完成、第 5 天可补完成”。该部分已被产品负责人明确替代，原文保留于本注记作为 Historical / Replaced Evidence；稳定 ID、优先级和位置不变。

> Source Governance（2026-08-22，DEC-133）：AC-F009-28 的历史 When/Then 为“从默认部门/岗位覆盖率统计结果进入发布并带入条件”。DEC-133 已将覆盖分析和发布入口移出工作台，发布统一从 M03 专项管理进入；旧入口保留于本注记作为 Historical / Replaced Evidence。稳定 ID、优先级和位置不变。

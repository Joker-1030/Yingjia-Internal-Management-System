# FLOW-09 M04-approval-lifecycle

- 原始行：2526
- 原始最近标题：F1 通用审批实例生命周期与动作
- 迁移方式：Mermaid block mechanical copy
- 当前定位：Current Product Flow；普通审批动作仅提交、通过、驳回；预设会签、提交时预设抄送、回调重试与失效处理人受控替换继续有效（DEC-163、DEC-187）。

```mermaid
flowchart TD
    A[提交申请] --> B{是否直接生效动作?}
    B -- 是 --> C[生成已通过实例并回调业务<br/>admin即时分配/总裁发起停用等]
    B -- 否 --> D[待接收/待审批]
    D --> E{处理动作}
    E -- 通过 --> F{还有下一节点?}
    F -- 是 --> D
    F -- 否 --> G{有计划生效日?}
    G -- 无 --> H[立即回调业务生效]
    G -- 有 --> I[已通过待生效]
    I --> J{到达生效日且校验通过?}
    J -- 失败 --> K[已通过-业务处理失败<br/>后台幂等重试,持续失败由admin受控重试]
    J -- 通过 --> H
    E -- 驳回 --> L[已驳回,业务恢复申请前状态]
    D -- 处理人失效 --> R[按受控规则替换处理人<br/>保留原应处理人与替换记录]
```

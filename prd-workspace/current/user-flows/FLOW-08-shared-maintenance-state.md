# FLOW-08 shared-maintenance-state

- 原始行：2178
- 原始最近标题：维系任务状态与完成认定
- 迁移方式：Mermaid block mechanical copy
- 当前定位：Current Mechanical Evidence；DEC-204 删除`补录审核中`，逾期补录校验成功后直接完成；未暂停的生日/节假日越过截止后进入已过期未完成，暂停中的生日/节假日按 DEC-200 越过截止后自动关闭并退出有效集合。

```mermaid
stateDiagram-v2
    [*] --> 待执行
    待执行 --> 已完成: 实际维系时间不晚于截止时间
    待执行 --> 当前逾期: 超过截止时间
    待执行 --> 待执行: 符合资格的暂停确认生效
    待执行 --> 待执行: 常规恢复并按当前职级周期重算截止
    待执行 --> 待执行: 生日/节假日于截止前恢复
    待执行 --> [*]: 生日/节假日暂停越过截止后关闭
    当前逾期 --> 已完成: 实际发生日在当前截止内且证明齐全并确认
    当前逾期 --> 已完成: 常规任意后续日期补完成，或其他类型仍在窗口
    当前逾期 --> 当前逾期: 常规持续未完成，无最晚窗口
    当前逾期 --> 已过期未完成: 生日/节假日/专项超过规则窗口
    待执行 --> 已过期未完成: 生日/节假日规则不允许补完成且越过截止
```

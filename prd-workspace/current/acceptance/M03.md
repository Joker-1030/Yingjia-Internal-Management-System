# M03 Acceptance Criteria

> Mechanical migration from the current full PRD. Stable IDs, priority, and row order are preserved. Product-owner-confirmed wording replacements are governed below and retain their prior wording as Historical / Replaced Evidence.

| AC 编号 | Given | When | Then | 优先级 |
| --- | --- | --- | --- | --- |
| AC-F009-38 | 打开关键人覆盖 KPI 发布或编辑表单 | 查看行业、集团、业务责任层级、目标公司、目标部门、目标岗位和关键人职级 | 七项均为单选且各自包含并默认选择对应“全部”选项；候选按行业 -> 集团 -> 业务责任层级 -> 公司 -> 部门 -> 岗位递进过滤，上游为“全部”时下游展示当前其余条件范围内正常候选的去重并集；不显示或保存覆盖维度字段，不支持多选 | P0 |
| AC-F009-39 | 目标范围下同时存在选择具体值的维度匹配/不匹配，以及选择“全部”的维度存在不同值的当前有效任职 | 发布并运行覆盖 KPI | 只对行业、集团、业务责任层级、公司、部门原子 ID、岗位原子 ID、职级中选择具体值的条件执行等值匹配且取 AND；选择“全部”的维度不限制，不按 OR、不按名称、不按别称匹配具体目标 | P0 |
| AC-F009-40 | KPI 七个目标条件均保持默认“全部”或仅部分维度选择具体值 | 点击发布 | “全部”作为有效选项不触发缺失校验；系统按发布人授权范围和所有具体条件计算目标公司集合，范围内没有正常客户公司时阻止创建并保留表单输入 | P0 |
| AC-F008-33 | 当前范围存在全部四类当前分类及取消/关闭执行记录 | 查看父任务总览和执行明细 | 已完成、待执行/暂停、当前逾期、已过期未完成四类互斥合计等于有效执行记录总数，取消/关闭不计入；父任务编号、子执行编号与各自详情一致 | P0 |
| AC-F008-34 | 任务在截止前、逾期后分别提交维系 | 完成任务 | 前者按期；后者按实际时间进入逾期补录或逾期补完成，三个时间和曾逾期事实可查；不创建审批对象 | P0 |
| AC-F008-35 | 已逾期任务倒填当前截止内时间 | 未填原因、未传证明或传入非法文件，再使用合法字段和文件确认提交 | 任一校验失败均不创建记录、不完成或续期；全部合法时直接形成正式维系记录并认定按期，原截止只保留审计且曾逾期事实继续保留 | P0 |
| AC-F008-36 | 专项维系或覆盖 KPI 执行项仍未完成 | 尝试申请单项延期或人工完成覆盖 KPI | 页面无入口，伪造请求返回状态冲突；截止时间仍等于专项结束时间，覆盖 KPI 仅由七项目标条件中具体值的组合覆盖事实自动完成，“全部”维度不限制 | P0 |
| AC-F008-37 | 同一任务执行记录生成调度重复执行或回调重试 | 查看有效执行记录 | 唯一键保证同一计划/事件只有一条有效执行记录，复用原父任务号和原子执行编号且不重复通知 | P0 |
| AC-F008-38 | 管理员代办 PM 或区域总监任务 | 填写代办原因并完成 | 执行人仍是原业务执行人，创建人/实际操作人显示管理员，两者及原因进入日志 | P0 |
| AC-F008-39 | 任一常规计划、生日年月、节日年度、专项维系或覆盖 KPI 首次建立正式父任务并生成执行记录 | 查看总览、执行明细与详情 | 父任务获得唯一 `MT` + 8 位任务编号；专项发布后与其一对一父任务共用该编号且不存在独立专项业务编号；执行记录获得 `任务编号-6位父内流水`，列表可按任务编号或任务执行记录编号搜索，详情分别显示且用户不能编辑 | P0 |
| AC-F008-40 | 同一父任务并发生成 30 条不同执行记录，且另有调度重试命中同一执行业务唯一键 | 查看父任务与编号序列 | 不重复建立父任务；不同执行记录子编号在父内不重号，重复业务返回既有执行记录及原父/子编号，失败事务允许断号 | P0 |
| AC-F008-41 | 一条执行记录完成、取消或关闭，随后又生成下一期执行记录 | 查询父任务及全部历史执行记录 | 父任务编号始终不变；历史子编号永久保留且不复用；下一期仅获得同一父任务下新的递增 6 位子编号 | P0 |
| AC-F008-42 | 父任务、执行记录和维系记录均发生过业务修改，随后列表成功加载或手动刷新 | 查看任务总览、执行明细、维系记录和各自详情 | 三张列表均不展示行级更新时间，并在表格右上角展示最近一次成功加载或刷新的“数据更新时间”；详情展示对象自身创建/更新时间，纯查看、消息已读和统计重算不改变对象更新时间 | P1 |
| AC-F008-43 | 常规维系任务已超过当前截止 400 天且从未完成 | 任务扫描多次后由 PM 提交实际时间为当天的维系记录 | 扫描期间始终为当前逾期且不存在补完成截止或下一期；提交成功后认定逾期后补完成，并按实际维系日期加当前职级周期生成唯一下一期，曾经逾期和首次逾期事实保留 | P0 |
| AC-F008-44 | 生日/节假日规则均固定不允许逾期补完成 | 任务越过截止并在第 5 天、第 8 天提交 | 单条任务从创建时开始，生日截止为生日当天、节假日为假期末日23:59:59；截止当天仍按既有资格执行，越过截止均进入已过期未完成；第 5 天和第 8 天均无补完成入口并拒绝提交，也不创建逾期补录记录 | P0 |

| AC-F008-45 | 发布后未开始、有效期内进度 60.0%、有效期内进度 100.0%、结束后进度 100.0% 和结束后进度不足 100.0% 各一条 | 查看专项列表、任务总览和父任务详情 | 专项不提供草稿状态；专项与唯一父任务分别同为待开始、进行中、进行中、已结束和已结束，任务主题使用同一状态；完成进度不改变时间生命周期，零执行项只显示空态而非第四个父任务状态 | P0 |
| AC-F008-46 | 同一关键人的常规任务长期当前逾期 | 分别让定时扫描、主动维系入口、页面列表处理该任务 | 定时扫描不转已过期且不建下一期；主动维系仍唯一匹配并可逾期补完成；已存在任务保留原截止与提醒行或当前逾期；列表持续标红并可进入处理 | P0 |
| AC-F008-47 | 常规任务当前逾期，随后关键人停用或调岗生效 | 查看原任务、目标责任计划和任务历史 | 停用按 M02 关键人结果关闭原任务，调岗按 M02 任职结果关闭原任务并在目标责任下重建唯一常规计划；两者均在任务历史保存来源、原因和结果，不保存审批流程编号或 M05 记录，也不把原任务伪记为已完成或已过期未完成 | P0 |
| AC-F008-48 | 同一区域存在省级和市级/区县级业务责任客户的有效关键人 | 运行常规、生日、节假日、专项维系及覆盖 KPI 生成并执行本人任务异常操作 | 省级业务责任客户五类任务类型的执行项均分配所属区域总监且不经过 PM，市级/区县级仍分配当前 PM；当前执行人或 admin 的延期、暂停、取消和补录在 M03 校验、确认后直接生效，不创建上级审批 | P0 |
| AC-F008-49 | 任务原截止为 3 月 1 日，延期后的当前截止为 3 月 10 日，系统在 3 月 11 日登记维系 | 实际维系时间分别填写 3 月 8 日和 3 月 11 日 | 3 月 8 日按逾期补录校验并确认后直接认定按期；3 月 11 日认定逾期后补完成；原截止只在延期历史和审计中展示 | P0 |
| AC-F008-50 | PM 或区域总监名下存在常规、事件、专项维系和覆盖 KPI 的未完成执行项、已完成项及历史项 | HR/人事或 admin 按该员工每个地市或区域中心选择合格接收人并确认停用，随后恢复员工 | 每个未完成旧执行项关闭并保留原执行人、稳定编号、状态和历史；同一父任务及任务编号下为对应接收人建立新的父内执行编号，新项沿用原当前截止，专项沿用专项结束时间。覆盖 KPI 关闭旧聚合项后按新责任合并到接收人已有同一 KPI 聚合项或新建一项；常规任务仅在新项完成后按完成日和届时职级周期续期。已完成和历史项不改写，恢复不重开旧项或取回任务责任 | P0 |
| AC-F008-51 | M10 同一批次包含成功且适用 M03 规则的对象、失败行，并发生一次任务生成失败后重试 | 查看导入结果和 M03 任务 | 成功且适用对象在导入成功后 5 分钟内生成唯一适用任务；失败行不生成任务；生成失败不回滚导入成功，重试复用同一业务唯一键且不重复生成父任务或执行记录 | P0 |
| AC-F008-52 | 当前权限范围内同时存在常规维系、专项维系、关键人覆盖 KPI、生日关怀和节假日关怀父任务；四类有限任务的进度包含 `--`、0.0%、40.0%、80.0% 和 100.0% | 查看 M03 任务总览并使用最低、最高及双边进度筛选 | 筛选项仅为“任务编号”“任务标题”“任务类型”“任务状态”“最低任务完成进度”“最高任务完成进度”，不展示目标范围或执行人筛选；常规维系进度固定显示 `-`，四类有限任务只显示 1 位小数百分比或 `--`，均不显示分子/分母或进度条；范围筛选包含边界、支持单边，并排除常规维系的 `-` 和有限任务零分母的 `--`；最低值高于最高值时不筛选并提示“最低进度不能高于最高进度”；列表继续不展示目标范围和行级更新时间，开始/结束时间及其他数据口径、权限和操作不变 | P1 |
| AC-F008-53 | 当前授权范围内某关键人定期维系计划包含四类有效执行记录及已取消、已关闭记录；完成一条常规维系后会生成下一期执行记录 | 查看任务总览和该常规维系的数据看板，完成一条任务并再次查看 | 任务总览在完成前后均显示 `-`，数据看板不展示“任务完成进度”指标卡；已完成、待执行 / 暂停、当前逾期、已过期未完成等数量继续按有效集合实时展示，已取消和已关闭不进入这些有效数量；完成与下一期生成规则不变，新执行记录进入后只更新对应执行数量，不产生常规维系百分比 | P0 |
| AC-F008-54 | 五类任务均已按 M09 规则或专项配置生成父任务和执行记录 | 从任务总览分别打开五类父任务，再从一级执行明细打开单条任务执行详情 | 五类父任务 Drawer 标题栏均只显示“任务详情”和关闭入口，并固定包含“任务详情、数据看板、执行明细”三个页签；名称区只显示任务名称和一处任务状态且无编号、类型或有效期副标题；任务编号、任务类型、开始时间、结束时间、更新时间及类型专属字段只在“任务详情”页签展示，字段区不重复任务名称和状态；生日月度父任务保留月初/月末，节假日父任务从自身首次创建时间到快照假期末日，角色范围或新增执行项不改开始时间；单条生日/节假日详情复用创建时间/当前截止表达开始/结束，保留原截止快照，详情分类统一称为“任务类型”；关键人定期维系计划不展示任务标题模板、执行要求或任务完成进度指标卡，但保留执行数量指标，其他类型按适用规则展示已有详情、范围和完成进度；Drawer 内执行明细保留表格但无操作列、任务详情或维系记录下钻，一级执行明细及单条任务执行详情继续展示渲染标题和保存的执行要求 | P1 |
| AC-F008-55 | 当前授权范围存在常规维系、专项维系、生日/节假日和覆盖 KPI 执行记录，并包含今日到期、未来第 1-7 日待执行、当前逾期、暂停、已过期未完成及曾经逾期记录 | 查看一级执行明细的筛选和列表，分别选择“当前逾期风险”“未来 7 日到期”，并查看覆盖 KPI 行 | 筛选仅展示任务编号、任务执行记录编号、任务标题、任务类型、关键人名称、客户公司、执行人、截止开始/结束日期和风险范围，四类当前分类仍由页签承载；风险范围只有全部、当前逾期风险和未来 7 日到期，前者仅命中常规/生日/节假日当前逾期且明确排除暂停任务，后者仅命中截止晚于今天且不晚于未来第 7 个自然日的待执行记录，两者互斥且不出现曾经逾期或无当前风险选项；列表列名为“执行目标”和“客户范围”，常规维系、专项维系、生日关怀和节假日关怀分别展示关键人与客户公司，覆盖 KPI 分别展示目标部门/岗位与 `共 N 个客户`，该数量不可点击且客户明细只从任务详情的数据看板进入 | P1 |
| AC-F008-56 | 当前存在未逾期且从未逾期、当前逾期、曾经逾期的常规/生日/节假日任务，以及专项维系和覆盖 KPI | 分别打开任务变更并伪造暂停提交；再让一条原本合法的暂停操作在最终确认前越过截止 | 只有未逾期且从未逾期的常规/生日/节假日任务显示并接受暂停；其余任务无暂停选项且请求被拒绝。确认生效前再次校验，已逾期任务不暂停并保持当前逾期，不创建审批实例或业务处理失败状态 | P0 |
| AC-F008-57 | 当前未逾期且从未逾期的常规、生日和节假日任务分别确认暂停 | 让常规任务到达恢复日；让生日任务在截止前恢复；让节假日任务暂停越过当前截止 | 三类任务暂停期间均归入待执行 / 暂停，不形成当前逾期、曾经逾期、首次逾期时间或健康风险。常规任务按恢复日、关键人恢复时当前职级及当前有效周期计算新当前截止至当日 23:59:59，并保留原截止及新旧截止、职级、周期历史；生日任务恢复待执行并沿用原当前截止；节假日任务自动关闭、退出有效集合和完成进度分子/分母，且不进入已过期未完成 | P0 |
| AC-F008-58 | 员工停用责任组中存在一条已确认暂停的常规、生日或节假日执行项 | HR/admin 选择合格接收人并完成该责任组交接，再分别让新项到达`暂停至`；同时模拟另一责任组交接失败后延迟补处理 | 旧项关闭且只读显示新父内编号的`后续执行项`；同一父任务下建立独立新项并只读显示旧编号的`来源执行项`，编号均不可点击。新项继承暂停原因和`暂停至`，其`原截止`与初始`当前截止`均等于交接时旧项当前截止，暂停期间不提醒、不可普通完成且不形成逾期或健康风险；到期后常规任务重算当前截止，生日/节假日按是否越过截止恢复或关闭。失败组不冻结原项暂停生命周期，补处理只交接当时仍未完成项；恢复员工不重开旧项或取回新项 | P0 |
| AC-F008-59 | 已启用生日/节假日规则已有已生成任务及尚未发送提醒 | admin 停用规则，跨过一个任务生成点和提醒节点后重新启用 | 停用期间不生成新任务、不发送尚未发送的规则提醒，已生成任务保留原状态、规则和时间快照且继续按既有操作资格处理；重新启用只作用于启用后符合条件的后续任务与提醒，不补建或补发停用期间错过的内容 | P0 |
| AC-F008-60 | 任务满足既有延期资格，当前执行人本人或 admin 代办 | 分别确认延期成功、取消确认、校验失败和重复提交 | 本人延期成功只页面反馈；admin 代办成功仅通知实际任务执行人一次，内容为任务及调整后的截止时间，不向主管广播；执行人是区域总监时按执行人身份接收。取消或失败不发成功消息，重复提交不重复通知，投递失败不回滚延期；专项/KPI 无单项延期及既有日期限制保持 | P0 |
| AC-F008-61 | 常规维系、生日或节假日任务满足既有暂停资格，由当前执行人本人或 admin 代办 | 分别确认暂停成功、取消确认、校验失败及重复提交 | 本人成功只页面反馈；admin 代办成功仅向实际任务执行人发送一次包含任务及“暂停至”日期的结果消息，不向主管广播；执行人是区域总监时按执行人身份接收。取消确认或失败不发成功消息，重复提交不重复通知，投递失败不回滚暂停；暂停期间仍停止任务提醒，既有暂停资格、期限及恢复/关闭规则保持 | P0 |
| AC-F008-62 | 任务符合既有主动取消资格，由当前执行人本人或 admin 代办 | 分别完成取消确认、放弃确认、校验失败及重复提交 | 本人成功只页面反馈；admin 代办成功仅向实际任务执行人发送一次说明任务已取消及取消原因的消息，不向主管广播，执行人是区域总监时按执行人身份接收。放弃确认或失败不发成功消息，重复提交不重复通知，投递失败不回滚取消；原因校验、二次确认、退出有效集合及历史保留不变，自动关闭和关联操作不套用此通知规则 | P0 |
| AC-F008-63 | 本人或 admin 操作的暂停任务分别为常规维系、生日关怀和节假日关怀 | 到达暂停至日期并满足既有恢复条件，再观察后续提醒节点 | 实际恢复待执行时不另发恢复消息，保留原任务/执行项身份并通过原任务/待办继续承接，不新建重复任务。常规按恢复时重算的当前截止、生日/节假日按原当前截止执行既有提醒；不因没有恢复消息而停止任务提醒。节令暂停越过截止仍按原规则自动关闭，不被错误恢复 | P0 |
| AC-F008-64 | 本人或 admin 操作暂停的生日/节假日任务仍处于暂停期间 | 越过当前截止并实际自动关闭，再检查任务、待办、消息及历史 | 不另发关闭消息；保留任务记录、关闭结果、原因和历史，不再进入待办，按既有规则退出有效集合和完成进度分子/分母，不形成逾期。未暂停节令越期仍按原规则处理，其他关闭来源不套用本条 | P0 |
| AC-F008-65 | 任务符合正常完成资格，由当前执行人本人或 admin 代办 | 分别完成提交、校验失败及重复提交 | 本人正常完成成功只页面反馈；admin 代办成功仅向实际任务执行人发送一次说明任务已完成的结果消息，不向主管广播，执行人是区域总监时按执行人身份接收。失败或重复提交不发/重复成功消息，投递失败不回滚完成；正式记录、完成认定和适用续期保持。逾期补录、逾期补完成不套用本条 | P0 |
| AC-F008-66 | 任务满足逾期补录资格，由本人或 admin 代办 | 分别提交成功、失败及重复提交 | 本人成功只页面反馈；admin 代办成功仅向实际任务执行人发送一次说明补录已完成及认定结果的消息，不向主管广播；失败或重复提交不发/重复通知，投递失败不回滚补录。逾期补完成不套用本条 | P0 |
| AC-F008-67 | 任务满足逾期补完成资格，由本人或 admin 代办 | 分别提交成功、失败和重复提交，检查消息与表单 | 本人成功仅页面反馈；admin 代办成功仅向实际任务执行人发送一条补完成及认定结果消息，不通知主管；失败页面显示具体原因并保留填写内容，不发失败消息；重复提交不重复通知，消息失败不回滚补完成；生日、节假日及覆盖 KPI 仍无该入口 | P0 |
| AC-F008-68 | 员工停用责任组为新执行人建立一条或多条 M03 执行项 | 检查新执行人、旧执行人和主管收到的结果 | 仅实际成功后新执行人收到交接结果：单项含任务、客户、当前截止和来源；同一成功责任组向同一新执行人交接多个任务时合并为一条，显示任务数和来源，不逐任务重复通知；不同组不合并，旧项关闭不逐项通知，失败组不发成功消息；任务交接结果不因消息失败而回滚 | P0 |
| AC-F009-20 | 发布人填写专项表单 | 新建、修改条件并提交超长标题、结束早于开始或允许补完成却不填截止 | 新建及编辑不展示责任人/影响预览，打开或修改条件时不实时计算责任人/人数/覆盖预览；新建按钮“确认发布”、编辑按钮“保存”，点击时执行必要目标匹配、责任分配及原校验；对应字段报错且不能发布，当前表单内容保留供修正，不创建专项、父任务、任务编号或执行记录 | P0 |
| AC-F009-21 | 所有目标维度为空 | 点击发布 | 展示当前全范围预计关键人数并要求显式勾选确认；未确认不能发布 | P0 |
| AC-F009-22 | 有效期内新增、调岗或修改职级使关键人进入/退出范围 | 等待增量匹配和小时兜底 | 5 分钟内增量更新，兜底无重复；每项保留规则版本与判定原因 | P0 |
| AC-F009-23 | 专项尚未结束或已结束 | 修改结束日期 | 结束前统一同步全部有效执行项并重算；结束后拒绝修改；单项无独立截止 | P0 |
| AC-F009-24 | 专项维系含各种完成认定和当前状态 | 查看详情看板并下钻 | 总完成率、按期完成率和四类当前分类均可与同一执行项集合核对；覆盖 KPI 页面不混用该维系完成率 | P0 |
| AC-F009-25 | 总裁选择一个行业、集团、市级业务责任层级、目标公司、目标部门、目标岗位和关键人职级发布覆盖 KPI | 提交时目标确认并正式发布 | 按七项组合展示分母、已覆盖、当前覆盖率和需新增数；只有公司、职级、部门与岗位同时匹配的当前有效关键人计入；发布后目标公司的当前责任人生成 1 条唯一 KPI 待办 | P0 |
| AC-F009-26 | 覆盖 KPI 有效期内目标公司最后一名符合目标职级/部门/岗位的关键人停用并使责任人覆盖率低于目标，随后新增合法关键人 | 等待增量重算 | 原责任人执行项重新打开原待办，不发送消息通知或创建重复任务，恢复达标后自动完成；始终复用同一执行项编号且记录失去/恢复达标时间 | P0 |
| AC-F009-27 | 覆盖 KPI 分别选择省级、市级、区县级业务责任客户 | 发布并查看执行人 | 省级业务责任客户执行人是区域中心主管，市级/区县级执行人是地市负责人 PM；无有效责任人的单位列为发布阻断项 | P0 |
| AC-F009-28 | 总裁、市场副总或 admin 查看覆盖 KPI 提交阶段目标确认或已发布专项详情 | 未点击发布，随后从 M03 专项管理进入“发布专项任务” | 仅查看不生成待办；填写行业、集团、业务责任层级、目标公司、稳定部门/岗位 ID、关键人职级、目标值、业务责任范围和有效期并正式发布后才生成任务；M01 工作台不提供覆盖分析或发布入口 | P0 |
| AC-F009-29 | 市场副总选择目标公司、目标部门、目标岗位和关键人职级发布 KPI | 提交、发布并查看执行明细 | 分子仅统计当前有效公司、任职部门原子 ID、岗位原子 ID 和职级同时等于目标的关键人所在单位；任一不匹配均不计入，按责任人生成唯一聚合待办 | P0 |
| AC-F009-30 | 发布人选择目标岗位但目标范围内没有所需标准岗位 | 尝试输入自由文本或直接发布 | 页面不提供自由文本或别称入口并提示先由 admin 在 M09 岗位页签维护岗位；接口拒绝未知岗位原子 ID，不生成专项或待办 | P0 |
| AC-F009-31 | 覆盖 KPI 发布表单在行业、集团、业务责任层级、公司、部门、岗位和职级中混合选择“全部”与具体值 | 提交或重新打开表单 | 七项单选及其“全部/具体值”语义均保留为同一组合快照；详情对无限制维度显示对应“全部”，不存在覆盖维度切换，不残留 OR 条件 | P0 |
| AC-F009-32 | 专项维系范围内同时有采购决策人和非采购决策人 | 分别选择全部、仅采购决策人、排除采购决策人并提交发布 | 提交时匹配人数和生成明细严格按当前字段值匹配且可核对，填写阶段不显示实时预览；该筛选不改变关键人普通列表的采购决策人优先排序 | P0 |
| AC-F009-33 | 责任人分母为 3，目标覆盖率为 66.7% | 已覆盖单位数从 2 变为 3 | 目标需覆盖数向上取整为 3；已覆盖 2 时不达标、3 时达标，页面百分比与整数判断同源且不因浮点误差提前完成 | P0 |
| AC-F009-34 | 同一专项发布请求超时并重试 | 查询专项、父任务和执行项 | 只存在一个专项、一个一对一父任务、二者共用的一个 `MT` 任务编号和一组唯一执行项；不生成独立专项业务编号，状态一致且重试不重复取号；首次成功创建记录创建时间，编辑和重试不改变，详情只读展示，列表和表单不增加该字段；新专项由既有执行任务进入责任人待办，不另行发送发布消息或创建重复待办 | P0 |
| AC-F009-35 | 待开始、进行中和已结束专项各一条，其中进行中与已结束均包含进度 100.0% 样例 | 查看专项列表并逐一按状态筛选，再打开详情和关联父任务 | 列表状态只展示待开始、进行中和已结束，不提供草稿状态；三种状态的专项与父任务按时间生命周期保持一致，进度 100.0% 不提前结束，列表、筛选和详情均使用同一任务编号且不出现双编号字段 | P0 |
| AC-F009-36 | 专项维系和覆盖 KPI 的有效期均已过 50%，且分别有已完成执行项和已达标责任人 | 计算重点动态的低进度阈值，随后将任一专项分母变为 0 | 专项维系使用总完成率，覆盖 KPI 使用已达标责任人数/有效责任人数；页面明确中文口径，分母为 0 显示 `--` 且不触发低进度动态 | P0 |
| AC-F009-37 | 编辑尚未结束的专项维系或关键人覆盖 KPI | 打开表单、修改目标条件、点击保存；另提交结束早于开始的日期 | 打开及改条件均无责任人/影响预览或实时预览计算；按钮仅为“保存”，提交执行必要匹配、责任分配和校验，无前置影响预览步骤；成功沿用“专项已更新”，日期无效不保存并保留输入 | P0 |
| AC-F010-22 | 新增维系记录 | 方式为其他但无说明，或摘要/反馈少于 5 字 | 字段级报错并保留其他输入，不写记录或任务 | P0 |
| AC-F010-23 | 单记录已有 9 个附件或 100 MB | 再传文件，或上传扩展名/MIME/文件头不一致文件 | 系统拒绝非法或超限文件，保留已通过附件，不返回永久地址 | P0 |
| AC-F010-24 | 附件关联成功但任务状态更新失败 | 提交任务完成 | 整体不生效，任务仍未完成，表单与临时附件可重试且不重复上传记录 | P0 |
| AC-F010-25 | PM 主动记录命中 0、1、2 条有效常规任务 | 分别提交 | 0 条仅保存；1 条同事务闭环；2 条阻止并告警，不任意选择任务 | P0 |
| AC-F010-26 | 已保存维系记录分别关联任务和未关联任务 | 打开“追加附件”入口并尝试修改关键人、任职/公司、维系方式、维系时间、摘要、反馈、下一步计划、关联任务或维系人，再追加附件 | 两类记录的全部业务字段均只读且伪造修改被拒绝；仅合法新附件追加成功，不改变任务完成认定、不重新匹配任务，已有附件不能删除或替换 | P0 |
| AC-F010-27 | 管理员独立录记录或代办任务 | 保存后查看详情 | 独立记录维系人为管理员；代办记录同时展示原业务维系人、管理员录入人和代办原因 | P0 |
| AC-F010-28 | 有附件权限和无附件权限用户分别打开同一图片、PDF 和 Office 附件 | 预览、下载、尝试删除/替换已有附件或为已保存记录追加附件 | 有权用户可在线预览图片/PDF，Office 显示元数据并通过 5 分钟单次签名地址下载；已有附件无删除或替换入口且伪造请求不生效，合法新附件只追加；无权请求统一 403 且不返回永久对象地址 | P0 |
| AC-F010-29 | 当前权限范围内存在不同关键人、客户公司、维系方式、维系人、维系日期及任务关联情况的正式维系记录 | 查看维系记录筛选并组合查询 | 筛选仅展示记录编号、关键人名称、客户公司、维系方式、维系人、维系开始日期、维系结束日期和关联任务；开始/结束日期共同表达维系日期范围，各条件只匹配自身字段并取交集，不展示关键人编号、沟通摘要、区域、城市、附件、创建日期或记录状态筛选 | P1 |

> Source Governance（2026-09-04 / 2026-09-08，DEC-202）：AC-F008-50 中“停用时全部未完成任务直接取消”的旧规则已被替代。当前关闭原员工旧执行项并在同一父任务下为责任接收人建立新执行项；恢复不重开旧项或取回责任。2026-09-08 已补充确认暂停旧项关闭、新项继承暂停、截止初始化、按类型恢复/关闭、失败组继续计时及来源/后续编号双向只读展示，并由 AC-F008-58 覆盖。

> Task exception Current Rule（2026-09-04，DEC-204）：AC-F008-33/34/35/47/48/49/53/55/56/57 与 AC-F009-24 已移除当前审批语义。延期、暂停、取消和逾期补录由 M03 完成既有校验与确认后直接生效；逾期补录直接形成正式记录并认定按期，`补录审核中`和`补录驳回`不再是 Current 状态。

> Source Governance（2026-08-22，DEC-163）：AC-F008-44 的历史 Given/Then 曾包含“一类允许 7 天补完成、第 5 天可补完成”。该部分已被产品负责人明确替代，原文保留于本注记作为 Historical / Replaced Evidence；稳定 ID、优先级和位置不变。

> Source Governance（2026-08-22，DEC-133）：AC-F009-28 的历史 When/Then 为“从默认部门/岗位覆盖率统计结果进入发布并带入条件”。DEC-133 已将覆盖分析和发布入口移出工作台，发布统一从 M03 专项管理进入；旧入口保留于本注记作为 Historical / Replaced Evidence。稳定 ID、优先级和位置不变。

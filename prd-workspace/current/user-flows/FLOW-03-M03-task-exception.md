# FLOW-03 M03-task-exception

- 原始行：861
- 原始最近标题：关键字段通俗释义
- 迁移方式：Mermaid block mechanical copy
- 当前定位：Current Product Flow；省级业务责任客户异常/逾期补录上提市场副总，角色空缺或继续上提时由总裁处理；市级/区县级业务责任客户由所属区域总监处理；DEC-187 移除撤回；暂停资格、生效复核、恢复和节令越期关闭按 DEC-200（DEC-163、DEC-180、DEC-200）

```mermaid
flowchart TD
    A[执行人申请延期/暂停/取消] --> B{任务对象层级}
    B -- 市级/区县级业务责任客户任务 --> C[所属区域总监审批]
    B -- 省级业务责任客户任务 --> D[市场副总审批,角色空缺时总裁]
    C --> E{结果}
    D --> E
    E -- 驳回 --> F[任务保持原状]
    E -- 通过 --> G{变更类型}
    G -- 延期 --> H[重算截止与提醒]
    G -- 取消 --> I[退出有效任务集合]
    G -- 暂停 --> J{生效时仍未逾期且从未逾期?}
    J -- 否 --> K[暂停不生效;任务保持当前逾期;<br/>M04 记录已通过-业务处理失败]
    J -- 是 --> L[常规/生日/节假日进入待执行或暂停;<br/>不形成逾期和健康风险]
    L --> M{暂停任务类型与时间}
    M -- 常规到恢复日 --> N[按恢复日+当前职级当前周期<br/>重算当前截止并恢复待执行]
    M -- 生日/节假日截止前恢复 --> O[沿用原当前截止并恢复待执行]
    M -- 生日/节假日越过截止 --> P[自动关闭并退出有效集合]
    Q[逾期补录申请] --> R{实际发生时间在当前截止内且证明齐全?}
    R -- 否 --> S[阻止提交]
    R -- 是 --> C
```

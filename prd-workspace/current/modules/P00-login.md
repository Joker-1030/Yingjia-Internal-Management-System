# P00 登录页

- 文档状态：Supporting Product Facts（Current）；不作为独立 PRD Source of Truth
- 功能范围：登录与账号状态
- 原始事实章节：旧完整 PRD 第 179-254 行

## 模块目标

- 让员工使用手机号、密码和图片验证码进入系统。
- 在账号不可用、凭证错误、验证码错误或锁定时提供明确且不泄露账号事实的反馈。

## 用户角色

- 未登录用户使用登录页。
- 登录成功后的默认页面和数据范围由员工当前系统角色及责任事实决定；HR/人事固定进入 M06“组织与员工”。HR/人事从无权页面执行返回时同样进入 M06，不返回无权的 M01 工作台。

## 核心对象

- 员工账号
- 手机号登录标识
- 密码
- 图片验证码
- 登录会话

## 页面说明

- 登录页
- 手机号、密码、图片验证码和登录按钮
- Demo 演示账号快捷填充仅属于 Demo，不属于正式产品入口

## 功能列表

- 输入登录凭证
- 显示或隐藏当前密码输入
- 提交登录
- 接收错误、锁定或账号不可用反馈

## 状态

- 正常
- 提交中
- 验证码错误或失效
- 手机号或密码错误
- 账号锁定
- 员工或账号停用

## Demo 登录演示约定

- 仅当前 Demo：图片验证码自动填入现有输入框，演示者无需手填。首次打开登录页、点击验证码刷新、每次提交后的验证码更新、退出登录返回时，输入框均同步为当前新验证码；点击演示账号快捷填充时也填入当前验证码。保留验证码图片、输入框及刷新入口。
- Demo 仍先校验提交的验证码；手动改错时提示“图片验证码错误，请重试”，刷新并自动填入新验证码。手机号或密码错误提示“手机号或密码错误”，不再要求手输验证码；其余凭证、账号可用性、失败计数和锁定处理按下文执行。
- 以下“关键登录规则”、正式登录 Flow 和 Acceptance 描述正式产品要求，其中验证码清空且不回填答案的要求在 Demo 中由本节明确例外替代，不能将 Demo 便利操作用于正式产品。
- 依据：Product Owner 于 2026-09-08 明确要求 Demo 验证码自动填充；延续 `DEC-095`：[`prd-workspace/decisions/DEC-095-立项阶段使用唯一完整沟通稿并将-Demo-定位为可点击原型.md`](../../decisions/DEC-095-立项阶段使用唯一完整沟通稿并将-Demo-定位为可点击原型.md) → “决策”中的 Demo 可固定并预填验证码约定。

## 关键登录规则

- 登录先校验图片验证码。验证码错误或失效时，只清空并刷新验证码，不累计密码错误次数；手机号和密码输入保留，密码恢复掩码。
- 验证码错误反馈只说明验证码错误或已失效，不展示、暗示或回填正确答案。
- 仅在图片验证码正确、账号存在且可用时校验密码并累计连续密码错误次数。连续第 5 次密码错误后，账号锁定 30 分钟；页面不展示剩余可尝试次数。
- 密码错误时保留手机号、清空密码并刷新图片验证码，使用不泄露账号存在性或正确凭证的通用错误反馈；该次错误计入连续密码失败次数。
- 锁定期间即使密码正确也拒绝登录；锁定到期后连续密码错误次数清零。账号不存在、员工或账号停用、账号已锁定或登录服务异常时，不累计该手机号的密码错误次数。
- 登录成功后，同一图片验证码立即失效。
- 完整分支见 [FLOW-01 P00 login](../user-flows/FLOW-01-P00-login.md)；锁定与登录交互验收见 [AC-F001-20、AC-F001-32、AC-F001-33、AC-F001-43、AC-F001-44](../acceptance/M06.md)。

## 退出登录规则

- 用户点击退出登录后，显示二次确认弹窗：标题为“确认退出登录”，正文为“退出后将返回登录页面，是否继续？”，按钮为“取消 / 确认退出”。
- 取消或关闭弹窗不改变当前登录状态；确认退出后结束当前会话，清除当前客户选择和本地敏感信息，并返回登录页。
- 退出弹窗不展示 Demo 环境、内存数据或刷新后恢复初始数据等演示说明；完整退出结果见 [AC-F001-46](../acceptance/M06.md)。

## 权限边界

- 登录失败提示不得泄露手机号是否存在或密码是否正确。
- 登录后权限由角色模板、数据范围和当前责任事实共同决定。

## 不包含

- 用户名登录
- 短信验证码或第三方登录
- 记住账号
- 生产会话或密码存储实现方式

## 关联治理

- Acceptance：登录历史稳定 AC 编号位于 [M06/F001 Acceptance](../acceptance/M06.md)，本次不重新编号。
- User flow：[FLOW-01 P00 login](../user-flows/FLOW-01-P00-login.md)

## 迁移边界

本文件与 `prd-workspace/current/PRD.md` 及其当前引用链共同构成 P00 的 Current Product Source。旧完整 PRD 和 Archive 仅为 Historical Evidence，不能补充或覆盖本文件未定义的当前行为；Current 来源没有唯一答案时标记 `PRODUCT DEFINITION GAP / PRODUCT CONFIRMATION REQUIRED`，不得回退历史资料自行补齐。

## 修订记录 / PRD Revision History

本表只记录直接影响 P00 的已确认产品变更；总 PRD 维护跨模块摘要。同日变更合并在一个单元格内，无法从正式记录可靠还原的历史不补造。

当前没有可从现有正式修订记录与 Git 产品 Diff 可靠回填的模块级变更；后续首次发生产品变更时新增日期行。

| 日期 | 修订内容 |
| --- | --- |
| 2026-09-08 | • Demo 登录验证码在进入、演示账号快捷填充和刷新后自动填入，每次提交及退出返回同步更新；错误提示不再要求手输验证码，正式产品验证码规则保持原定义。 |
| 2026-09-07 | • HR/人事登录成功及从无权页面返回时固定进入 M06“组织与员工”，不落到无权的 M01 工作台。<br>• 验证码错误保留手机号和掩码密码，只清空并刷新验证码，不计密码失败且不得暴露正确答案；密码错误保留手机号、清空密码、刷新验证码并计入密码失败。 |
| 2026-09-02 | • 集中说明图片验证码校验顺序、密码错误计数、连续 5 次失败锁定 30 分钟、锁定期行为、到期清零及验证码失效规则，不改变现有登录行为。<br>• 退出登录二次确认弹窗正文改为“退出后将返回登录页面，是否继续？”，保留“确认退出登录”标题、“取消 / 确认退出”按钮及既有退出结果，移除 Demo 环境说明。<br>• 明确 P00 未定义行为不得回退旧完整 PRD 或 Archive 补齐；缺失事实按 Product Definition Gap 处理。 |
